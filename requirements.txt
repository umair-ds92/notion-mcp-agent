# Core agent framework
autogen-agentchat==0.4.*
autogen-ext[openai,mcp]==0.4.*

# Web server (FastAPI replaces Flask for native async support)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# Config
python-dotenv>=1.0,<2.0

# Ngrok tunnel (optional)
pyngrok>=7.0,<8.0